--- a/src/evdev_axes.c	2006-04-13 22:53:30.000000000 +0400
+++ b/src/evdev_axes.c	2008-07-15 16:32:31.000000000 +0400
@@ -231,6 +231,10 @@
 
     EvdevAxesRealSyn (pInfo, 1);
     state->abs->count = 0;
+
+    /* Prepare for some axis missing in the next report */
+    for (i = 0; i < ABS_MAX; i++)
+    	state->abs->v[!n][i] = state->abs->v[n][i];
 }
 
 static void
@@ -278,9 +282,6 @@
 	state->abs->v[n][-map] = ev->value;
 
     state->abs->count++;
-
-    if (!state->sync)
-	EvdevAxesAbsSyn (pInfo);
 }
 
 void
@@ -300,9 +301,6 @@
 	state->rel->v[-map] -= ev->value;
 
     state->rel->count++;
-
-    if (!state->sync)
-	EvdevAxesRelSyn (pInfo);
 }
 
 int
@@ -537,7 +535,13 @@
         return !Success;
 
     for (i = 0; i < axes; i++) {
-	xf86InitValuatorAxisStruct(device, i, 0, 0, 0, 0, 1);
+    	if (i < 2)
+	    xf86InitValuatorAxisStruct(device, i, 0, -1, 1, 1, 1);
+	else
+	    xf86InitValuatorAxisStruct(device, i,
+				       state->abs->min[i],
+				       state->abs->max[i],
+				       1, 1, 1);
 	xf86InitValuatorDefaults(device, i);
     }
 
